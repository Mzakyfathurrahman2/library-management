---
phase: 01-core-inventory-database
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - apps/api/src/routes/authors.ts
  - apps/api/src/routes/books.ts
  - packages/shared/src/schemas/inventory.ts
autonomous: true
requirements: [Task 1.3, FR-2.1, FR-2.2, FR-2.3]
---

<objective>
Develop the RESTful API endpoints for managing the book catalog and authors.

Purpose: Enable the librarian to perform CRUD operations on the library inventory.
Output: Validated REST endpoints for Authors, Books, and BookCopies.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-inventory-database/01-02-SUMMARY.md
@.planning/phases/01-core-inventory-database/01-03-SUMMARY.md
@.planning/phases/01-core-inventory-database/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CRUD API</name>
  <files>apps/api/src/routes/authors.ts, packages/shared/src/schemas/author.ts</files>
  <action>
    Create Zod schemas for Author creation/update in `packages/shared`.
    Implement `GET /api/authors`, `POST /api/authors`, `PUT /api/authors/:id`, `DELETE /api/authors/:id`.
    Apply `requireRole('LIBRARIAN')` to write operations (per D-05).
  </action>
  <verify>
    <automated>npx vitest apps/api/src/routes/authors.test.ts</automated>
  </verify>
  <done>Authors can be managed via REST API by librarians.</done>
</task>

<task type="auto">
  <name>Task 2: Books and Copies CRUD API</name>
  <files>apps/api/src/routes/books.ts, packages/shared/src/schemas/book.ts</files>
  <action>
    Create Zod schemas for Book and BookCopy management.
    Implement REST endpoints for Books (CRUD).
    Implement nested endpoints or separate endpoints for BookCopies (e.g., `POST /api/books/:id/copies`).
    Ensure BookCopies use the simple "status" field (per D-03).
  </action>
  <verify>
    <automated>npx vitest apps/api/src/routes/books.test.ts</automated>
  </verify>
  <done>Books and their copies can be managed via REST API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Role-based check required for all write operations. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Elevation of Privilege | Inventory Write | mitigate | requireRole('LIBRARIAN') middleware. |
</threat_model>

<verification>
Verify that a student cannot delete a book (403) but a librarian can.
</verification>

<success_criteria>
Full CRUD capabilities for the inventory catalog are available to authorized librarians.
</success_criteria>

<output>
Create `.planning/phases/01-core-inventory-database/01-04-SUMMARY.md` when done
</output>
