---
phase: 01-core-inventory-database
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/seed.ts
  - docker-compose.yml
  - .env
autonomous: true
requirements: [Task 1.2]
must_haves:
  truths:
    - "PostgreSQL is running via Docker"
    - "Prisma schema reflects the library domain (Books, Authors, Members, Copies)"
    - "Initial seed data is present in the database"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Relational data model"
    - path: "docker-compose.yml"
      provides: "Database infrastructure"
  key_links:
    - from: "apps/api/src/index.ts"
      to: "PrismaClient"
      via: "instantiation"
---

<objective>
Configure the persistence layer using PostgreSQL and Prisma, and seed the database with initial inventory.

Purpose: Provide a reliable and type-safe data store for the library's catalog and members.
Output: A running PostgreSQL instance and a synchronized Prisma schema with seed data.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-core-inventory-database/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Prisma Schema and Infrastructure</name>
  <files>apps/api/prisma/schema.prisma, docker-compose.yml, .env</files>
  <action>
    Create a docker-compose.yml for a PostgreSQL 16+ instance.
    Define the Prisma schema with models: Author, Book, BookCopy (per D-03: status Available/Borrowed), and Member.
    Ensure appropriate relations (e.g., Book -> Author, Book -> BookCopy).
    Configure DATABASE_URL in .env.
  </action>
  <verify>
    <automated>docker compose up -d && npx prisma validate --schema apps/api/prisma/schema.prisma</automated>
  </verify>
  <done>PostgreSQL is running and the Prisma schema is valid.</done>
</task>

<task type="auto">
  <name>Task 2: Database Migration and Seeding</name>
  <files>apps/api/prisma/seed.ts, apps/api/package.json</files>
  <action>
    Run `npx prisma migrate dev` to apply the schema to the database.
    Implement a seed script in TypeScript that populates the DB with at least 5 books, 3 authors, and 2 members (1 Librarian, 1 Student).
    Update apps/api/package.json with "prisma": {"seed": "ts-node prisma/seed.ts"}.
  </action>
  <verify>
    <automated>npx prisma db seed</automated>
  </verify>
  <done>Database is migrated and contains initial test data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> DB | Trust boundary; use parameterized queries (enforced by Prisma). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | SQL Injection | mitigate | Prisma ORM uses parameterized queries by default. |
</threat_model>

<verification>
Connect to PostgreSQL and verify tables exist and contain the seeded rows.
</verification>

<success_criteria>
PostgreSQL is accessible and schema matches the research recommendations.
</success_criteria>

<output>
Create `.planning/phases/01-core-inventory-database/01-02-SUMMARY.md` when done
</output>
