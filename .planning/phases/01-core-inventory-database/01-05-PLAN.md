---
phase: 01-core-inventory-database
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - apps/web/src/components/InventoryTable.tsx
  - apps/web/src/views/InventoryView.tsx
  - apps/web/src/styles/inventory.css
autonomous: true
requirements: [Task 1.4, FR-2.1, FR-2.2, FR-2.3]
---

<objective>
Build the librarian's inventory management interface with a high-density, performant data table.

Purpose: Provide an efficient way for librarians to browse and edit the book catalog.
Output: A React 19 inventory dashboard with sorting, filtering, and optimistic updates.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-inventory-database/01-04-SUMMARY.md
@.planning/phases/01-core-inventory-database/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement High-Density Table Component</name>
  <files>apps/web/src/components/InventoryTable.tsx, apps/web/src/styles/inventory.css</files>
  <action>
    Create a reusable Table component using Vanilla CSS.
    Implement `table-layout: fixed` and `sticky` headers for performance (per D-07, Research).
    Add support for sortable columns and basic text filtering (per D-08).
  </action>
  <verify>
    <automated>npx vitest apps/web/src/components/InventoryTable.test.tsx</automated>
  </verify>
  <done>Performant table UI is ready for inventory data.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory Management View with Optimistic Updates</name>
  <files>apps/web/src/views/InventoryView.tsx</files>
  <action>
    Fetch book and author data from the API.
    Implement inline editing for book titles/authors using React 19's `useOptimistic` hook for instantaneous feedback.
    Integrate with the backend CRUD API created in Plan 04.
  </action>
  <verify>
    <automated>npx vitest apps/web/src/views/InventoryView.test.tsx</automated>
  </verify>
  <done>Inventory view allows efficient management with a "spreadsheet-like" feel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> API | UI must handle 403 errors gracefully if role is insufficient. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | UI State | mitigate | useOptimistic ensures local state reflects intent, but server remains source of truth. |
</threat_model>

<verification>
Verify sorting by title, filtering by ISBN, and that optimistic updates rollback on API failure.
</verification>

<success_criteria>
Librarians can view and manage the entire inventory from a single, responsive dashboard.
</success_criteria>

<output>
Create `.planning/phases/01-core-inventory-database/01-05-SUMMARY.md` when done
</output>
