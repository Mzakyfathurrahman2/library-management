---
phase: 01-core-inventory-database
plan: 06
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - apps/api/src/routes/members.ts
  - apps/web/src/views/MembersView.tsx
  - apps/web/src/components/MemberRegistrationForm.tsx
autonomous: true
requirements: [Task 1.5, FR-1.1, FR-1.2]
---

<objective>
Implement member management capabilities for librarians, including student registration.

Purpose: Allow librarians to manage the user base and onboard new students.
Output: Member CRUD API and a registration/management UI.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-inventory-database/01-03-SUMMARY.md
@.planning/phases/01-core-inventory-database/01-05-SUMMARY.md
@.planning/phases/01-core-inventory-database/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Member Management API</name>
  <files>apps/api/src/routes/members.ts</files>
  <action>
    Implement CRUD endpoints for Members (restricted to LIBRARIAN role).
    Include password hashing using `bcrypt` for new registrations.
    Ensure Student ID, Name, and Email are mandatory fields (per FR-1.1).
  </action>
  <verify>
    <automated>npx vitest apps/api/src/routes/members.test.ts</automated>
  </verify>
  <done>Backend can create and manage student/librarian accounts.</done>
</task>

<task type="auto">
  <name>Task 2: Member UI (Registration and Listing)</name>
  <files>apps/web/src/views/MembersView.tsx, apps/web/src/components/MemberRegistrationForm.tsx</files>
  <action>
    Create a registration form with client-side validation using shared Zod schemas.
    Implement a member listing table (reusing the high-density table component from Plan 05).
    Connect the UI to the Member API.
  </action>
  <verify>
    <automated>npx vitest apps/web/src/views/MembersView.test.tsx</automated>
  </verify>
  <done>Librarians can register and manage members through the dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> DB | Sensitive data (passwords) must be hashed before storage. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Information Disclosure | Member List | mitigate | requireRole('LIBRARIAN') ensures only authorized staff see student data. |
| T-01-06-02 | Tampering | Password Reset/Set | mitigate | Use bcrypt with appropriate cost factor. |
</threat_model>

<verification>
Verify student registration with valid/invalid data and role enforcement for the member list.
</verification>

<success_criteria>
Librarians can successfully onboard new students and view the current member directory.
</success_criteria>

<output>
Create `.planning/phases/01-core-inventory-database/01-06-SUMMARY.md` when done
</output>
