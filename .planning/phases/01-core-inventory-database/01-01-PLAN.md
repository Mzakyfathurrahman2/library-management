---
phase: 01-core-inventory-database
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/shared/package.json
  - packages/shared/src/index.ts
  - apps/api/package.json
  - apps/api/src/index.ts
  - apps/web/package.json
  - apps/web/src/main.tsx
autonomous: true
requirements: [Task 1.1]
must_haves:
  truths:
    - "Monorepo is initialized with npm workspaces"
    - "Shared package exists and is importable by api and web apps"
    - "Express and React 19 skeletons are runnable"
  artifacts:
    - path: "package.json"
      provides: "Monorepo workspace configuration"
    - path: "packages/shared/src/index.ts"
      provides: "Shared types and schemas"
    - path: "apps/api/src/index.ts"
      provides: "Backend entry point"
    - path: "apps/web/src/main.tsx"
      provides: "Frontend entry point"
  key_links:
    - from: "apps/api/package.json"
      to: "packages/shared"
      via: "workspace dependency"
    - from: "apps/web/package.json"
      to: "packages/shared"
      via: "workspace dependency"
---

<objective>
Establish the monorepo structure and scaffold the backend and frontend applications.

Purpose: Provide a unified development environment and share validation logic across tiers.
Output: A working monorepo with Express (API) and React 19 (Web) skeletons.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-inventory-database/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Monorepo and Shared Package</name>
  <files>package.json, packages/shared/package.json, packages/shared/src/index.ts, tsconfig.json</files>
  <action>
    Initialize a root package.json with workspaces: ["apps/*", "packages/*"].
    Create packages/shared with TypeScript support.
    Export a placeholder Zod schema (e.g., AppConfigSchema) from packages/shared/src/index.ts to verify imports.
    Per Research: Use "packages/shared" for single source of truth.
  </action>
  <verify>
    <automated>npm install && ls packages/shared/package.json</automated>
  </verify>
  <done>Root and shared package configurations exist and link correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold API and Web Applications</name>
  <files>apps/api/package.json, apps/api/src/index.ts, apps/web/package.json, apps/web/src/main.tsx, apps/web/index.html</files>
  <action>
    Scaffold apps/api using Express 5.x and TypeScript.
    Scaffold apps/web using React 19 and TypeScript (Vite recommended).
    Add @library/shared as a workspace dependency to both apps.
    Implement a simple health-check endpoint in API and a "Hello World" in Web that imports a type from Shared.
  </action>
  <verify>
    <automated>npm run build --workspaces</automated>
  </verify>
  <done>Both applications build successfully and reference the shared package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Shared -> Apps | Code sharing is internal; trust is high. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-SC | Tampering | npm installs | mitigate | Research audit confirms packages; manual check if SUS. |
</threat_model>

<verification>
Ensure `npm install` works in root and `npm run build` passes for all workspaces.
</verification>

<success_criteria>
Monorepo is ready for feature development with shared type safety.
</success_criteria>

<output>
Create `.planning/phases/01-core-inventory-database/01-01-SUMMARY.md` when done
</output>
