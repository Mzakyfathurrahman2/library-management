---
phase: 01-core-inventory-database
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - apps/api/src/middleware/auth.ts
  - apps/api/src/routes/auth.ts
  - apps/api/src/index.ts
  - .env
autonomous: true
requirements: [Task 1.1, FR-1.3]
user_setup:
  - service: "JWT"
    why: "Session security"
    env_vars:
      - name: "JWT_SECRET"
        source: "Generate a random 32+ char string"
---

<objective>
Implement the authentication system using JWTs and secure HttpOnly cookies.

Purpose: Secure the system and distinguish between Librarian and Student roles.
Output: Auth middleware, login/logout endpoints, and CSRF protection.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-inventory-database/01-01-SUMMARY.md
@.planning/phases/01-core-inventory-database/01-02-SUMMARY.md
@.planning/phases/01-core-inventory-database/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Auth Middleware and JWT Handling</name>
  <files>apps/api/src/middleware/auth.ts, apps/api/src/utils/jwt.ts</files>
  <action>
    Implement JWT signing and verification using `jsonwebtoken`.
    Create a `requireAuth` middleware that extracts JWT from HttpOnly cookies (per D-02).
    Create a `requireRole(role: string)` middleware to enforce role-based access (per D-01, FR-1.2).
    Use `cookie-parser` for secure cookie handling.
  </action>
  <verify>
    <automated>npx vitest apps/api/src/middleware/auth.test.ts</automated>
  </verify>
  <done>Middleware correctly validates JWTs from cookies and checks roles.</done>
</task>

<task type="auto">
  <name>Task 2: Auth Routes and CSRF Protection</name>
  <files>apps/api/src/routes/auth.ts, apps/api/src/index.ts</files>
  <action>
    Implement `POST /api/auth/login`: validates credentials (bcrypt), sets HttpOnly cookie.
    Implement `POST /api/auth/logout`: clears the cookie.
    Integrate `csrf-csrf` for double-submit cookie CSRF protection.
    Register routes and middleware in `apps/api/src/index.ts`.
  </action>
  <verify>
    <automated>npx vitest apps/api/src/routes/auth.test.ts</automated>
  </verify>
  <done>Authentication endpoints are functional and secured against CSRF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted boundary; validate JWT and CSRF tokens. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Spoofing | Login | mitigate | Bcrypt hashing for passwords; JWT for sessions. |
| T-01-03-02 | Tampering | Session Cookie | mitigate | HttpOnly, Secure, and SameSite=Lax/Strict flags. |
| T-01-03-03 | Tampering | Cross-Site Request Forgery | mitigate | csrf-csrf double-submit cookie pattern. |
</threat_model>

<verification>
Attempt to access a protected route without a cookie (401), with a valid cookie (200), and with a wrong role (403).
</verification>

<success_criteria>
Users can log in and receive a secure session cookie that is verified by the server.
</success_criteria>

<output>
Create `.planning/phases/01-core-inventory-database/01-03-SUMMARY.md` when done
</output>
